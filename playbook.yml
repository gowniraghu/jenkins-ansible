---
- name: task for install httpd service
  hosts: all
  become: true

  tasks:
    - name: install httpd service
      yum:
        name: httpd
        state: present
        
    - name: remove tree package
      yum:
        name: tree
        state: absent
