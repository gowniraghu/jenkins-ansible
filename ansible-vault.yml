---
- name: Ansible Vault Example
  hosts: all
  become: true
  vars_files:
    - vault.yml

  tasks:
    - name: clone a git repository
      git:
        repo: https://github.com/gowniraghu:[{password}}/git_course.git
      debug: /opts/git_course/